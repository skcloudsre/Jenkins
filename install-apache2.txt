========================================================================================
httpd Install
========================================================================================
sudo apt update
#Update packages

sudo apt install apache2 -y
#Install Apache

sudo systemctl status apache2
#Check Apache status

sudo systemctl enable apache2
#Enable Apache at boot

aws ec2 describe-instances `
  --filters "Name=key-name,Values=test-kp" `
  --query "Reservations[].Instances[].[InstanceId,SecurityGroups[].GroupId]" `
  --output table `
  --region ap-south-1
#Get your EC2 security group ID

aws ec2 describe-security-groups `
  --group-ids sg-0f3b3561899a9909f `
  --query "SecurityGroups[].IpPermissions" `
  --output json `
  --region ap-south-1
#check your ports

aws ec2 authorize-security-group-ingress `
  --group-id sg-0f3b3561899a9909f `
  --protocol tcp `
  --port 80 `
  --cidr 0.0.0.0/0 `
  --region ap-south-1
#allow the port number 80

scp -i "C:\Users\SESHU_VANI\Downloads\test-kp.pem" "C:\Users\SESHU_VANI\Downloads\index.html" ubuntu@35.154.205.142:/home/ubuntu/
#copy files from windows to ubunu machine
